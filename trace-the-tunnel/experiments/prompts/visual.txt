You are playing a browser game at http://localhost:5050.

Look at the page. There is a curved blue/grey tunnel-shaped region drawn
on a canvas, with a green dot at one end and a red dot at the other end.

Goal: move the mouse from the green dot to the red dot, dragging the
cursor through the inside of the tunnel without ever crossing the
boundary. If the cursor leaves the tunnel the attempt fails and the
same tunnel automatically resets after a brief pause.

Play all 10 tunnels (IDs 0 through 9). Try up to 3 attempts per tunnel;
if all 3 fail, move on to the next tunnel anyway.

Switching tunnels:
  window.__tunnelGame.loadTunnel(N)        # N = 0..9
  window.__tunnelGame.getState()           # "ready" | "tracing" | "done_success" | "done_fail"
  window.__tunnelGame.getTunnelId()        # current tunnel id

After each tunnel — whether the final attempt succeeded or you've used
your 3 attempts — save the latest session:

  (async () => {
    const data = window.__tunnelGame.getSessionData();
    const resp = await fetch('/api/save_trajectory', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(data),
    });
    return await resp.json();
  })()

Then move on to the next tunnel.

Rules:
- You must rely on what you see in screenshots. There is no API that
  exposes the tunnel's path, centerline, control points, or boundary.
  The only way to know where the tunnel goes is to look at it.
- Figure out the rest yourself.

Tools available: browser_navigate, browser_take_screenshot,
browser_snapshot, browser_click, browser_evaluate.
